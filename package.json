{
  "name": "pi-packages",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm --workspaces run build",
    "setup": "npm install && npm run build && for d in packages/*/; do pi install \"./$d\"; done"
  }
}
